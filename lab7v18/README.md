# Лабораторна робота №7 — Обробка IO/мережевих помилок та патерн Retry

**Варіант 18: Збереження кешу**

## Тема та мета

**Тема:** обробка типових помилок вводу/виводу та мережевих помилок за допомогою `try-catch-finally`, а також реалізація патерну Retry.

**Мета:** навчитися обробляти IO/мережеві винятки і реалізувати патерн Retry з експоненційною затримкою для підвищення відмовостійкості застосунків.

## Опис виконання завдання (варіант 18)

- **`FileProcessor.SaveCache(path, cacheData)`** — імітує `IOException` перші 4 рази, потім успіх.
- **`NetworkClient.UploadCache(url, cacheData)`** — імітує `HttpRequestException` перші 2 рази, потім успіх.
- **`shouldRetry`** — повторювати лише для `IOException` та `HttpRequestException`.

### Файли

- **`FileProcessor.cs`** — робота з файлами, метод `SaveCache`.
- **`NetworkClient.cs`** — мережеві запити, метод `UploadCache`.
- **`RetryHelper.cs`** — узагальнений клас з методом `ExecuteWithRetry<T>` (експоненційна затримка + логування).
- **`Program.cs`** — демонстрація в `Main`: сценарії тимчасових помилок + вибіркова обробка через `shouldRetry`.

### Реалізовано

- `try-catch` для IO та мережевих винятків;
- узагальнений `RetryHelper.ExecuteWithRetry<T>(Func<T>, int, TimeSpan, Func<Exception,bool>)`;
- **експоненційна затримка** `initialDelay * 2^retryAttempt`;
- логування кожної спроби та причини невдачі;
- `shouldRetry` для вибіркової обробки винятків.

## Приклад запуску

```
=== Сценарій 1: Збереження кешу (FileProcessor.SaveCache) ===
Очікується IOException перші 4 рази, потім успіх.

  [Retry] Спроба 1/6 невдала (IOException: ...). Чекаємо 100 мс...
  [Retry] Спроба 2/6 невдала (IOException: ...). Чекаємо 200 мс...
  [Retry] Спроба 3/6 невдала (IOException: ...). Чекаємо 400 мс...
  [Retry] Спроба 4/6 невдала (IOException: ...). Чекаємо 800 мс...
  [FileProcessor] Кеш збережено у 'cache/data.bin' (13 байт).
Результат: збереження кешу завершилось успішно.

=== Сценарій 2: Вивантаження кешу (NetworkClient.UploadCache) ===
Очікується HttpRequestException перші 2 рази, потім успіх.

  [Retry] Спроба 1/4 невдала (HttpRequestException: ...). Чекаємо 100 мс...
  [Retry] Спроба 2/4 невдала (HttpRequestException: ...). Чекаємо 200 мс...
  [NetworkClient] Кеш вивантажено на 'https://api.example.com/cache' (13 байт).
Результат: вивантаження кешу = True.

=== Сценарій 3: Вибіркова обробка через shouldRetry ===
Кидаємо ArgumentException — shouldRetry має заборонити повтор.

  [Retry] Спроба 1: ArgumentException не підлягає повтору — кидаємо далі.
Результат: операція припинена без повторів — ArgumentException: Некоректний аргумент — не підлягає повтору.
```

## Як запустити та зробити скріншот

Термінал у папці `lab7v18` (де лежить `lab7v18.csproj`):

```bash
cd lab7v18
dotnet run
```

З іншої папки:

```bash
dotnet run --project lab7v18/lab7v18.csproj
```

Перевірка без запуску:

```bash
dotnet build      # зібрати, побачити помилки компіляції
dotnet clean      # очистити bin/obj
```

Зроби скріншот консолі з виводом і встав нижче замість коментаря.

## Відповіді на контрольні питання

1. **Які типи винятків найчастіше при роботі з файлами та мережею?**
   Файли: `IOException`, `FileNotFoundException`, `DirectoryNotFoundException`, `UnauthorizedAccessException`.
   Мережа: `HttpRequestException`, `SocketException`, `TimeoutException`.

2. **Принцип патерну Retry. Коли доцільний?**
   Операція повторюється кілька разів, якщо тимчасово падає (мережеві збої, зайнятий файл, короткочасна недоступність сервера). Доцільний саме для *тимчасових* (transient) помилок; для постійних (некоректний аргумент, немає прав) — недоцільний.

3. **Як реалізувати експоненційну затримку?**
   Затримка зростає з кожною спробою: `initialDelay * 2^retryAttempt` (100 → 200 → 400 → 800 мс). Це зменшує навантаження на ресурс, що відновлюється.

4. **Для чого потрібен делегат `shouldRetry`?**
   Дозволяє вибірково вирішувати, чи повторювати операцію для конкретного типу винятку — повторюємо лише "тимчасові" (`IOException`, `HttpRequestException`), а решту одразу прокидаємо далі без марних спроб.

<!-- Сюди додати скріншот реального виводу програми -->
